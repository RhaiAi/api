openapi: 3.0.3
info:
  title: RHAI API
  description: >
    Catálogo oficial de endpoints da plataforma RHAI.  
    Apenas **Edge Functions** ficam expostas no Swagger.  
    RPCs e tabelas internas não aparecem.  
  version: 1.0.0
  contact:
    name: Time RHAI
    email: suporte@rhai.com

servers:
  - url: https://gmtexatrdgqaymnknopd.supabase.co/functions/v1
    description: Supabase RHAI (produção)
  - url: http://localhost:54321/functions/v1
    description: Supabase RHAI (local dev)

tags:
  - name: Busca Ocupacional
    description: Endpoints relacionados à super busca e autocomplete de ocupações.
  - name: Cursos
    description: Endpoints para CRUD de cursos.
  - name: Ocupação ↔ Cursos
    description: Endpoints para vincular cursos às ocupações.

paths:
  # ============================================================
  # 🔹 BLOCO: BUSCA OCUPACIONAL
  # ============================================================
  /search-occupation:
    post:
      tags: [Busca Ocupacional]
      summary: Super Busca Ocupacional (RHAI)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [search_term]
              properties:
                search_term:
                  type: string
                  example: "engenheiro de prompt"
      responses:
        '200':
          description: Resposta padronizada
        '400':
          description: Erro de validação
        '500':
          description: Erro interno do servidor

  /rhai_edge_search_occupation_list:
    get:
      tags: [Busca Ocupacional]
      summary: Autocomplete de ocupações
      description: >
        Retorna lista de ocupações **ativas** com busca parcial (ILIKE)  
        em `occupations.name` e `occupation_similar_names_new.normalized_name`.  
        Usado em dropdowns/autocomplete no Lovable.
      parameters:
        - name: query
          in: query
          description: Texto de busca parcial
          schema: { type: string }
          example: "diretor de criacao"
        - name: limit
          in: query
          schema: { type: integer, default: 50, minimum: 1, maximum: 200 }
        - name: offset
          in: query
          schema: { type: integer, default: 0, minimum: 0 }
      responses:
        '200':
          description: Lista de ocupações encontradas
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:   { type: integer, example: 750 }
                        name: { type: string,  example: "Diretor de criação" }
                        register_code: { type: string, example: "2531-30" }
        '500':
          description: Erro interno

  # ============================================================
  # 🔹 BLOCO: CURSOS
  # ============================================================
  /rhai_edge_courses_new/list:
    get:
      tags: [Cursos]
      summary: Listar cursos
      parameters:
        - name: p_query
          in: query
          schema: { type: string }
        - name: p_limit
          in: query
          schema: { type: integer, default: 50 }
        - name: p_offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Lista de cursos

  /rhai_edge_courses_new/insert:
    post:
      tags: [Cursos]
      summary: Inserir curso
      requestBody:
        required: true
        content:
          application/json:
            schema:
              properties:
                p_name: { type: string }
                p_qualification_type_id: { type: integer }
                p_country_id: { type: integer }
      responses:
        '200':
          description: Curso inserido
        '400':
          description: Erro de validação
        '500':
          description: Erro interno

  /rhai_edge_courses_new/update:
    put:
      tags: [Cursos]
      summary: Atualizar curso
      responses:
        '200':
          description: Curso atualizado
        '400':
          description: Erro de validação
        '500':
          description: Erro interno

  /rhai_edge_courses_new/delete/{id}:
    delete:
      tags: [Cursos]
      summary: Remover curso
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Curso removido
        '404':
          description: Curso não encontrado

  /rhai_edge_search_courses_autocomplete:
    get:
      tags: [Cursos]
      summary: Autocomplete de cursos
      description: >
        Retorna cursos ativos para autocomplete.  
        Usa busca `ILIKE` em `name` e `name_normalized`.  
        Paginado por `limit` e `offset`.
      parameters:
        - name: query
          in: query
          schema: { type: string }
          example: "administração"
        - name: limit
          in: query
          schema: { type: integer, default: 20 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Lista de cursos encontrados
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:   { type: integer, example: 1 }
                        name: { type: string, example: "Administração" }
                        qualification_type: { type: string, example: "Bacharelado" }
                        country_name: { type: string, example: "Brasil" }
        '500':
          description: Erro interno

  # ============================================================
  # 🔹 BLOCO: OCUPAÇÃO ↔ CURSOS
  # ============================================================
  /rhai_edge_occ_edu/list:
    get:
      tags: [Ocupação ↔ Cursos]
      summary: Listar vínculos Ocupação ↔ Curso
      parameters:
        - name: p_occupation_ids
          in: query
          schema: { type: array, items: { type: integer } }
        - name: p_course_ids
          in: query
          schema: { type: array, items: { type: integer } }
      responses:
        '200':
          description: Lista de vínculos

  /rhai_edge_occ_edu/insert:
    post:
      tags: [Ocupação ↔ Cursos]
      summary: Criar vínculo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              properties:
                p_occupation_id: { type: integer }
                p_course_id: { type: integer }
                p_mandatory: { type: boolean }
                p_relevance: { type: string, enum: [AR,MR,BR] }
                p_notes: { type: string }
      responses:
        '200':
          description: Vínculo criado
        '400':
          description: Erro de validação
        '500':
          description: Erro interno

  /rhai_edge_occ_edu/update:
    put:
      tags: [Ocupação ↔ Cursos]
      summary: Atualizar vínculo
      responses:
        '200':
          description: Vínculo atualizado
        '400':
          description: Erro de validação
        '500':
          description: Erro interno

  /rhai_edge_occ_edu/delete/{id}:
    delete:
      tags: [Ocupação ↔ Cursos]
      summary: Remover vínculo (soft delete)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Vínculo removido
        '404':
          description: Vínculo não encontrado
